export interface Movie {
    rank: Number,
    id: string,
    description: string,
    title: string,
    rating: string,
    year: Number,
    thumbnail: string,
    image: string,
    trailer: string
}